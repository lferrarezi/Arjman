{
  "name": "browser-extension",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run sync-version && esbuild background.js --bundle --outfile=dist/background.js --format=cjs",
    "sync-version": "node ./scripts/sync-manifest-version.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "npm run sync-version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "version": "1.1.2",
  "devDependencies": {
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@arjman/core": "^1.0.0"
  }
}
